package com.example.tasks.saver.dto.enums;

public enum TasksStatus {
    PROJECT, //during creation task
    RUNNING, //adding/removing of operation
    FINISHED; //in case all operation are in the status Done
    }
